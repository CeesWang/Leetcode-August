class Solution {
    public int mincostTickets(int[] days, int[] costs) {
        int[] dp = new int [days.length + 1];
        for (int i = 0; i < days.length; ++i) { // day tickets 
            dp[i + 1] = dp[i] + costs[0];    
        }
        return dp[days.length];
    }
}
